# Deputy runtime state (per-repo). Keep BACKLOG.md and templates tracked.
.deputy/

# Local staging area for files pending deletion (never committed).
to-be-deleted/

# IDE / editor project files (local, per-developer).
.idea/
