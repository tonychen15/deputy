1. task list, this task list is a editabl file, user can edit this fail to add more task or remove a pending task. Or user can run /todo "a task" to add a task to this file and schedule a task. You can follow the /home/tong/Documents/Tech/Distributed-Systems/Core-Technologies/research.sh to see how this feature works.
2. When a task is added to the to-do list, then this skill should pick it up based on the urgency and importance rule and work on it with the waypoint skill defined by https://github.com/tonychen15/claude-waypoint. A tasked is added with -ui meaning urgent and important, otherwise without any option, then it means not urgent and important.
3. During this procedure, if the plan, design or commit should be reviewed, then call the cross-llm review skill https://github.com/tonychen15/xReview.
4. When a task is scheduled, depends on the task's complexity, the app should choose different claude's LLM. If Claude's quota is exhausted, this app sould check other LLM providers' status to route the task to different LLM providers. 
